'use strict';var _api = require('./api');var api = _interopRequireWildcard(_api);function _interopRequireWildcard(obj) {if (obj && obj.__esModule) {return obj;} else {var newObj = {};if (obj != null) {for (var key in obj) {if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key];}}newObj.default = obj;return newObj;}}

describe('archiveTrack', function () {
  it('should call axios.patch correctly', function () {
    var patch = jest.mock('axios', function () {return jest.fn();});
    var track = { id: 42 };

    api.archiveTrack(track);

    expect(patch).toHaveBeenCalledWith('/tracks/1/', {
      archived: true });

  });
});